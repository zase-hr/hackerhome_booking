import React from 'react';
import ReactDOM from 'react-dom';

class Booking_Module extends React.Component {
  constructor(props){
    super(props);
  }

  render() {
    return (
      <div></div>
    );
  }
}
ReactDOM.render(<Booking_Module />, document.getElementById('booking'));